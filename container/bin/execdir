#!/bin/execlineb -s1

ifelse -n
{ /usr/bin/test -d $1 }
{ exit 1 }

pipeline { s6-ls -a $1 }
forstdin SCRIPT
foreground {
  importas -u SCRIPT SCRIPT
  if { /usr/bin/test -f $SCRIPT }
  if { /usr/bin/test -x $SCRIPT }
  exec $SCRIPT
}

exec -- $@